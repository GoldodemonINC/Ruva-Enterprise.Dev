fn sum_array(arr: [i32; 5]) -> i32 {
    let mut sum = 0
    for i in 0..5 {
        sum += arr[i]
    }
    return sum
}

fn main() {
    let numbers = [10, 20, 30, 40, 50]
    println!("sum = {}", sum_array(numbers))
}
