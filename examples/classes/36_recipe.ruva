class Recipe {
    pub let name: string,

    pub fn new() -> Self {
        return Self { name: "" }
    }

    pub fn get_name(&self) -> string { return self.name.clone() }
}

fn main() {
    let obj = Recipe::new()
    println!("Recipes class created")
}
