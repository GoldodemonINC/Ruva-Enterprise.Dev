class Song {
    pub let title: string,

    pub fn new() -> Self {
        return Self { title: "" }
    }

    pub fn get_title(&self) -> string { return self.title.clone() }
}

fn main() {
    let obj = Song::new()
    println!("Playlist class created")
}
