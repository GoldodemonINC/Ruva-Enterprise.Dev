class Meal {
    pub let name: string,

    pub fn new() -> Self {
        return Self { name: "" }
    }

    pub fn get_name(&self) -> string { return self.name.clone() }
}

fn main() {
    let obj = Meal::new()
    println!("Meals class created")
}
