fn main() {
    println!("Hello, {}!", "world")
    let msg = format!("x = {}, y = {}", 1, 2)
    println!("{}", msg)

    let nums = vec![1, 2, 3, 4, 5]
    for n in nums {
        println!("{}", n)
    }

    assert!(1 == 1)
    assert_eq!(2 + 2, 4)
    assert_ne!(1, 2)
}
